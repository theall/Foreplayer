set(FOREPLAYERLIB_PLUGIN_DIST_PREFIX ${FOREPLAYERLIB_OUTPUT_PATH}/plugins)
option(FOREPLAYERLIB_BACKEND_PLUGINS "Build foreplayerlib backend plugins" ON)

if(FOREPLAYERLIB_BACKEND_PLUGINS)
    add_subdirectory(backend)
endif(FOREPLAYERLIB_BACKEND_PLUGINS)
